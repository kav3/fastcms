version: '3.9'

services:
  vue3:
    build: .
    container_name: fastcmsadmin
    restart: always
    networks: 
      - pn 
    environment:
      NODE_ENV: production
    ports:
      - 8080:8080
networks:
  pn:
    external: 
      name: private-network